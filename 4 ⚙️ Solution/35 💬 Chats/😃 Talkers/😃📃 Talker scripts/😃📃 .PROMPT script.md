
```yaml
# Create the a prompt ID
- EVAL|.UUID >> $prompt

- SAVE|
    Broker: $.Chat.Broker
    Chat: $.Chat.Chat
    Chat: $.Chat.PublicKey
    Prompt: <prompt-uuid>
    
    # Prompt
    Format: INFO
    Statement: {$emoji} {$:Statement}
    Options: $:Options
    Details: $:Details
    Appendix: $:Appendix

    Prompt: $prompt
    TTL: $ttl

- SEND:
    To: $.Chat.Broker
    Subject: Prompt@Broker
    Chat: $.Chat.Chat$
    Prompt: $prompt
    TTL: $ttl
```