@startuml Hook

box Talker 😃 domain #FFFFFA
    participant "Script 📃" as script
    participant ".HOOK 📃" as hook
    participant "Holders 🪣" as holders
    participant "Hooks 🪣" as hooks
    participant "Engine ⚙️" as engine
end box



activate script
    script -> hook: HOOK {hook}

    activate hook
        hook -> hooks: SAVE 💾 {hook, $.Script, $.Holders.Holder}

        hook -> hook: PARALLEL {$.Holders}
        activate hook
            hook -> holders: SAVE 💾 {hook, holder, value}
        deactivate hook

        hook -> script: RETURN { }
    deactivate hook

    engine <--  : REEL 🎣 {hook, return}
    activate engine
        script <- engine : RETURN {return}
    deactivate engine

@enduml