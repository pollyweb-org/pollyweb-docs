# 😃🔩 Talker `{.Filter}` function

> Part of [{Function} 🐍](<../📃 basics/Function 🐍.md>)

<br/>


1. **What is the .Filter function?**

    `{.Filter}`
    * is a [{Function} 🐍](<../📃 basics/Function 🐍.md>) 
    * that filters a list.

    ---
    <br/>

1. **What's the .Filter syntax?**

    ```yaml
    .Filter($list, [filters])
    ```

    Inputs|Type|Details
    |-|-|-
    |`$list`| list | Items to be filtered
    |`[filters]`| list | Boolean evaluations

    ---
    <br/>

1. **What are example outputs?**

    |Scenario|$list|[conditions]|Output
    |-|-|-|-
    |Empty list| `[]` | `[A:1]` | `[]`
    |Empty filters| `[{A:1}]` | `[]` | 🚫 Blocked
    |Object lists| `[{A:1},{A:2}]` | `[A:1]` | `[A:1]`
    |Object lists| `[{A:1},{A:2}]` | `[A:3]` | `[]`

    ---
    <br/>
   
1. **What's an example of .Filter?**

    Consider a list `$items`.

    ```yaml
    # Items
    | ID | Price | SupID  |
    | -- | ----- | ------ |
    |  1 |    10 |      A |
    |  2 |    20 |      X |
    |  3 |    30 |      X |
    |  4 |    40 |      X |
    ```

    |Input 1 | Input 2 |Output
    |-|-|-
    |`$items` |`SupID:X` | Items 2, 3, and 4
    |`$items` |`SupID.Is(X)` <br/> `Price.AtLeast(30)`  | Items 3 and 4

    ---
    <br/>

1. **What are examples for functions for filtering?**

    |Function|Purpose
    |-|-
    |{{.Contains}}  | A list property contains a given value?
    |{{.Differs}}   | A property differs from a given value?
    |{{.Equals}}    | A property equals a given value?
    |{{.In}}        | A property is in a given list?
    |{{.Is}}        | A property is similar to a given value?
    |{{.IsAbove}}   | A property is above a given value?
    |{{.IsAtLeast}} | A property is equal or above a value?
    |{{.IsBelow}}   | A property is equal or below a value?
    |{{.IsEmpty}}   | A property is empty?
    |{{.NotIn}}     | A property is not in a given list?

    ---
    <br/>