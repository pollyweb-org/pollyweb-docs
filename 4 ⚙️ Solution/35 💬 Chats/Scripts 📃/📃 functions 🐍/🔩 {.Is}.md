# 😃🔩 Talker `{.Is}` function

> Part of [{Function} 🐍](<{Function} 🐍.md>)

<br/>


1. **What is a .Is command?**

    `{.Is}`
    * is a [{Function} 🐍](<{Function} 🐍.md>) 
    * that verifies if a value is similar to another
    * following the `~=` convention of [`ASSERT`](<../📃 holders 🧠/ASSERT 🚦/🚦 ASSERT ⌘ cmd.md>).

    ---
    <br/>

1. **What's the .Is syntax?**

    ```yaml
    .Diff(<value>, <list>)
    ```

    | Inputs | Purpose | Examples
    |-|-|-
    | `<value>`  | Value to search for | `1` `ABC` 
    | `<list>`   | List to search in |  `[1,ABC]`  

    ---
    <br/>

1. **What are examples of .Is?**

    Here's a [Script 📃](<../📃 commands ⌘/Script 📃/📃 Script.md>)

    ```yaml
    📃 Examples

    # With static values
    - IF|.Is(1, 1):
        INFO|It is!    

    # With placeholders
    - EVAL|1 >> $p
    - IF|$p.Is(1):
        INFO|It is!    
    ```
    Commands: [`IF`](<../📃 control ▶️/IF ⤵️/⤵️ IF ⌘ cmd.md>) [`INFO`](<../../Prompts 🤔/🤔📢 Prompt status/INFO ℹ️/INFO ℹ️ prompt.md>)

    ---
    <br/>
