# 😃🔩 Talker `{.Distinct}` function

> Part of [{Function} 🐍](<{Function} 🐍.md>)

<br/>


1. **What is a .Distinct command?**

    `{.Distinct}`
    * is a [{Function} 🐍](<{Function} 🐍.md>) 
    * that outputs the distinct values of a list.

    ---
    <br/>

1. **What's the .Distinct syntax?**

    ```yaml
    .Distinct($list)
    ```

    ---
    <br/>

1. **What's an example of .Distinct?**

    Here's a [Script 📃](<../📃 commands ⌘/Script 📃/📃 Script.md>)

    ```yaml
    📃 Example with a Holder: 
    
    # $list = [A,B,C]
    - RETURN|$list.Distinct() # [A,B]
    ```
    Commands: [`RETURN`](<../📃 for control ▶️/RETURN ⤴️/⤴️ RETURN ⌘ cmd.md>)

    ```yaml
    📃 Example with EVAL: 
    
    # $list = [A,B,C]
    - EVAL|.Distinct >> $distinct 
        $list
    
    # Return the distinct
    - RETURN|$distinct # [A,B]
    ```
    Commands: [`RETURN`](<../📃 for control ▶️/RETURN ⤴️/⤴️ RETURN ⌘ cmd.md>)

    
    ---
    <br/>
