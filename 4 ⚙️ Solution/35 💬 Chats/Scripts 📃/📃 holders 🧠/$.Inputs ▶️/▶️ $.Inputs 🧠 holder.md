# 😃▶️ Talker `$.Inputs` holder

> Part of [Script 📃](<../../📃 basics/Script 📃.md>)

 
<br/>


1. **What is an $.Inputs command?**

    `$.Inputs`
    * is a built-in [Holder 🧠](<../../📃 basics/Holder 🧠.md>)
    * that contains all the inputs from the current [`RUN`](<../../📃 control ▶️/RUN ▶️/▶️ RUN ⌘ cmd.md>) command.

    For flexibility, it's also available as an `.Inputs` function.

    ---
    <br/>

1. **What are examples of $.Input?**

    Here's a [Script 📃](<../../📃 basics/Script 📃.md>)

    ```yaml
    📃 Example:
    - RUN|MyMethod:
        A: 1
        B: 2
    ```

    ```yaml
    📃 MyMethod:
    - INFO|A is {$A}
    - INFO|A is also {$.Inputs.A}
    ```

    ```yaml
    📃 Assert as holder:
    - ASSERT|$.Inputs:
        AllOf: A, B
    ```
    
    ```yaml
    📃 Assert as function:
    - ASSERT|.Inputs:
        AllOf: A, B
    ```
    Commands: [`ASSERT`](<../ASSERT 🚦/🚦 ASSERT ⌘ cmd.md>) [`INFO`](<../../../Prompts 🤔/🤔📢 Prompt status/INFO ℹ️/INFO ℹ️ prompt.md>) [`RUN`](<../../📃 control ▶️/RUN ▶️/▶️ RUN ⌘ cmd.md>)


    ---
    <br/>
