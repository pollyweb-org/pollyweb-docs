@startuml Chat@Broker


participant "Host 🤗" as host 

box Broker 🤵 domain #FFFFFA
    participant "Chat@ 📃" as broker
    participant "Chatters 🪣" as chatters
    participant "Chats 🪣" as chats
end box

activate host
host -> broker: Chat 🚀 {chatID}

    activate broker
        broker -> chatters: READ 🧲
        broker -> chats: 
        broker -> host: RETURN {details}
    deactivate broker

deactivate host

@enduml 
