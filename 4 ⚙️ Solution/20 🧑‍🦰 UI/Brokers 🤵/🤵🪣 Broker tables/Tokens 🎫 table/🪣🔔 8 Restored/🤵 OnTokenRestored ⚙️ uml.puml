@startuml OnTokenRestored

participant "Wallet 👱" as wallet

box Broker 🤵 domain #FFFFFA
    participant "Pop 🐌" as pop
    participant "Tokens 🪣" as tokens
    participant "OnRestored 🔔" as restored
end box

'participant "Notifier 📣" as notifier


wallet --> pop: Pop 🐌

activate pop
    wallet --> pop: {remove}
    wallet --> pop: {restore}
    pop -> tokens: SAVE 💾 {RESTORED}
deactivate pop

activate tokens
    tokens --> restored: RESTORED 🔔
deactivate tokens

@enduml 
