# 😃🔩 Talker `{.Format}` function

> Part of [{Function} 🐍](<../../../35 💬 Chats/Scripts 📃/Function 🐍.md>)

## FAQ

1. **What is the .Format function?**

    `{.Format}`
    * is a [{Function} 🐍](<../../../35 💬 Chats/Scripts 📃/Function 🐍.md>) 
    * that formats every item of a [List 🧠 holder](<../../📃 Holders 🧠/🧠 Holder types/List holders.md>) 
    * into a new [List 🧠 holder](<../../📃 Holders 🧠/🧠 Holder types/List holders.md>).


    ---
    <br/>

1. **What's the .Format syntax?**

    ```yaml
    .Format(holder, format)
    ```

    Inputs|Details | Example
    |-|-|-|
    |`holder`| List to format | `ABC` `1,2` `{A:1},{A:2}`
    |`format`| Format to apply | `{X:A, Y:B}`
    

    ---
    <br/>


   
1. **How to format a list of objects?**

    ```yaml
    # Let's turn this      # Into this
    ┌────┬────┬───┐        ┌───────┬──────┐
    │ A  │ B  │ C │        │ Alpha │ Beta │
    ├────┼────┼───┤        ├───────┼──────┤
    │ 10 │ 11 │ X │        │   10  │  11  │
    │ 20 │ 21 │ Y │        │   20  │  21  │
    └────┴────┴───┘        └───────┴──────┘
    ```

    Here are the [Scripts 📃](<../../../35 💬 Chats/Scripts 📃/Script 📃.md>).

    ```yaml
    📃 Option 1 - Directly:
    - INFO|$list.Format({Alpha:A,Beta:B})
    ```

    ```yaml
    📃 Option 2 - with EVAL:
    - EVAL|.Format >> $output:
        - $list
        - Alpha: A
          Beta: B
    - INFO|$output
    ```

    ```yaml
    📃 Option 3 - cleaner format:
    # Calls .Format internally
    - EVAL|$list >> $output:
        Alpha: A
        Beta: B
    - INFO|$output
    ```
    Commands: [`EVAL`](<../../📃 Commands ⌘/⌘ for holders 🧠/EVAL 🧮/🧮 EVAL ⌘ cmd.md>) [`INFO`](<../../📃 Prompts 🤔/🤔 Status ⚠️ prompts/INFO ℹ️/INFO ℹ️ prompt.md>)


    ---
    <br/>

