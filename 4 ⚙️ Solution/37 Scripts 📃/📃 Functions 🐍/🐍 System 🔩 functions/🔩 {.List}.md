# 😃🔩 Talker `{.List}` function

> Part of [{Function} 🐍](<../../../35 💬 Chats/Scripts 📃/Function 🐍.md>)



## FAQ

1. **What is the .List function?**

    `{.List}`
    * is a [{Function} 🐍](<../../../35 💬 Chats/Scripts 📃/Function 🐍.md>) 
    * that extracts properties from a list of objects
    * into a new list.


    ---
    <br/>

1. **What's the .List syntax?**

    ```yaml
    .List(list, properties)
    ```

    Inputs|Details | Example
    |-|-|-|
    |`list`| List of objects | `{A:1},{A:2}`
    |`properties`| Name or list of properties to extract | `A` `A,B`
    

    ---
    <br/>


   

1. **How to create a value array from a list of objects?**
  
    ```yaml
    # Let's turn this      
    ┌────┬────┬───┐        # Into this
    │ A  │ B  │ C │        # Same as [10,20]
    ├────┼────┼───┤        ┌────┐
    │ 10 │ 11 │ X │        │ 10 │
    │ 20 │ 21 │ Y │        │ 20 │
    └────┴────┴───┘        └────┘
    ```
    
   
    Here are the [Scripts 📃](<../../../35 💬 Chats/Scripts 📃/Script 📃.md>).

    ```yaml
    📃 Option 1 - Directly:
    - INFO|$list.List(A)
    ```

    ```yaml
    📃 Option 2 - Alternative:
    # Calls .List internally
    - INFO|$list(A)
    ```

    ```yaml
    📃 Option 3 - with EVAL:
    - EVAL|.List >> $output:
        - $list
        - A
    - INFO|$output
    ```

    ```yaml
    📃 Option 4 - with EVAL but cleaner:
    # Calls .List internally
    - EVAL|$list >> $output: 
        A 
    - INFO|$output
    ```

    ```yaml
    📃 Option 5 - Simplest:
    # Calls .List internally
    - INFO|$list.A 
    ```


    Commands: [`EVAL`](<../../📃 Commands ⌘/⌘ for holders 🧠/EVAL ⬇️/⬇️ EVAL ⌘ cmd.md>) [`INFO`](<../../📃 Prompts 🤔/🤔 Status ⚠️ prompts/INFO ℹ️/INFO ℹ️ prompt.md>)



    ---
    <br/>



1. **How to create a value array from a list of objects?**
  
    ```yaml
    # Let's turn this      
    ┌────┬────┬───┐        # Into this, same as
    │ A  │ B  │ C │        # [[10,11],[20,21]]
    ├────┼────┼───┤        ┌────┬────┐
    │ 10 │ 11 │ X │        │ 10 │ 11 │
    │ 20 │ 21 │ Y │        │ 20 │ 21 │
    └────┴────┴───┘        └────┴────┘
    ```
    
   
    Here are the [Scripts 📃](<../../../35 💬 Chats/Scripts 📃/Script 📃.md>).

    ```yaml
    📃 Option 1 - Directly:
    - INFO|$list.List(A,B)
    ```

    ```yaml
    📃 Option 2 - Simplest:
    # Calls .List internally
    - INFO|$list(A,B)
    ```

    ```yaml
    📃 Option 3 - with EVAL:
    - EVAL|.List >> $output:
        - $list
        - A,B
    - INFO|$output
    ```

    ```yaml
    📃 Option 4 - with EVAL but cleaner:
    # Calls .List internally
    - EVAL|$list >> $output: 
        - A 
        - B
    - INFO|$output
    ```

    Commands: [`EVAL`](<../../📃 Commands ⌘/⌘ for holders 🧠/EVAL ⬇️/⬇️ EVAL ⌘ cmd.md>) [`INFO`](<../../📃 Prompts 🤔/🤔 Status ⚠️ prompts/INFO ℹ️/INFO ℹ️ prompt.md>)



    ---
    <br/>