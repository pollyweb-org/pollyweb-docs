@startuml VERIFY Schema 🧩

box Talker 😃 domain #FFFFFA
    participant "Script 📃" as script
    participant "VERIFY ⌘ Schema 🧩" as verify
end box
participant "Graph 🕸" as graph


activate script
    script -> verify: VERIFY 🔐 {data, schema}

activate verify
    
    verify -x script: ASSERT 🚦 .IsSchema

    verify -> graph: SEND 📬 {Schema@Graph 🚀 code}
    activate graph
        graph -> verify: {schema}
    deactivate graph

    verify -x script: invalid schema 💥

    verify -> script: RETURN ✅ 

deactivate verify

          
@enduml