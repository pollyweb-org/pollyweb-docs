@startuml VERIFY Token 🎫

box Talker 😃 domain #FFFFFA
    participant "Script 📃" as script
    participant "VERIFY ⌘" as verify
end box
participant "Graph 🕸" as graph
participant "Broker 🤵" as broker


activate script
    script -> verify: VERIFY 🔐 {token}

activate verify
    
    verify -x script: not-started, expired 💥

    verify -> graph: SEND 📬 {Schema@Graph 🚀 code}
    activate graph
        graph -> verify: {schema}
    deactivate graph

    verify -x script: invalid context 💥

    verify -> broker: SEND 📬 {Status@Broker 🚀 token, issuer}
    activate broker
        broker -> verify: {status}
    deactivate broker

    verify -x script: not-active 💥

    verify -> script: RETURN ✅ 

deactivate verify

          
@enduml