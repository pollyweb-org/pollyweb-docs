@startuml BIND


box Vault 🗄️ domain #FFFFFA
    participant "Script 📃" as script
    participant "BIND ⌘" as bind
    participant "Binds 🪣" as binds
end box

participant "Broker 🤵" as broker
'participant "Wallet 👱" as wallet


activate script
    script -> bind: BIND {schema}
deactivate script

activate bind
    bind -> binds: 💾 {OFFERED, schema}
activate binds
    binds -> bind: {bind}
    bind -> bind: WAIT 🧘 {bind.ID}
deactivate bind
    binds --> broker: Bind 🐌
deactivate binds

activate broker
    broker --> binds: Bound 🐌
deactivate broker

activate binds
    binds -> bind: RACE 🏁 {bind}
deactivate binds

activate bind
    bind -> script: {bind}
deactivate bind

activate script
    

@enduml 